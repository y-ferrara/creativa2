apiVersion: v1
kind: Service
metadata:
  name: reviews-service
  namespace: cdps-18
  labels:
    app: reviews
    service: reviews
spec:
  ports:
  - port: 9080
    name: http
  selector:
    app: reviews
---